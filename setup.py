from setuptools import setup

setup(name='tensorairspace',
      version='0.0.1',
      install_requires=[
          'tensorflow==2.2.0',
          'gym',
          'tqdm',
          'matplotlib',
          'matlab',
      ]
      )
